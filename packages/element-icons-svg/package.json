{
  "name": "@teleskop-labs/element-icons-svg",
  "version": "0.0.4",
  "files": [
    "/src/**/*.svg"
  ],
  "sideEffects": false,
  "scripts": {
    "optimize": "svgo --config ./svgo.config.js -f ./src -o ./src",
    "clean": "rimraf .turbo && rimraf node_modules"
  },
  "devDependencies": {
    "svgo": "3.0.2"
  }
}
